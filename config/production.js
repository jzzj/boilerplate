module.exports = {
	isDebug: false,
	isOnline: true,
	/* 
	if you want to use cdn, then indicate this cdn field, and deal with uploading resources to cdn yourself.
	res-dump-plugin will build your resource to path.staticServerPath, and hash file/url, prefix the reference url.
	*/
	//cdn: "http://cdn.example.com/some/path",
}